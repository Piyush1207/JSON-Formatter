# JSON-Formatter
A fast and clean JSON Formatter built with React.js and Tailwind CSS. Paste your raw or minified JSON, format it with one click, and explore the output in an interactive tree view or raw text — without modifying your original input. Supports dark/light theme, minify, validate, copy to clipboard, and indent control.
